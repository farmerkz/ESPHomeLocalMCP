# [DEPRECATED] Этот файл оставлен для справки.
# Текущий способ запуска — через Python venv. См. setup.sh и README.md.

services:
  esphome-mcp:
    build: .
    container_name: esphome-mcp
    restart: unless-stopped
    init: true
    # Поддерживаем контейнер в активном состоянии для приема команд
    command: ["sleep", "infinity"]
    # Обеспечиваем доступ к localhost хоста для Apple Silicon
    extra_hosts:
      - "host.docker.internal:host-gateway"
